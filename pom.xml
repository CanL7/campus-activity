<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>org.SpringData</groupId>
    <artifactId>campus-activity-api</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>campus-activity-api</name>
    <description>campus-activity-api</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>17</java.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>

        <!--
            JWT：jjwt 0.13.0，拆成三个 artifact
              jjwt-api      —— 编译期用到的接口（Jwts、Claims、SecretKey），放 compile scope
              jjwt-impl     —— 真正的实现，只在运行时需要，所以 runtime scope
              jjwt-jackson  —— JSON 序列化实现（0.12 起必须显式加，以前藏在 jjwt 里）
            注意：不要再写 io.jsonwebtoken:jjwt（0.9 时代的聚合包，已废弃）
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.13.0</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.13.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.13.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>4.0.1</version>
        </dependency>

        <!--
            PageHelper 6.1.1 —— 分页插件

            注意：加的是「核心包」，不是 pagehelper-spring-boot-starter。
              原因：starter 的自动配置类是给 Spring Boot 2/3 写的，本项目是 Spring Boot 4，
                    自动配置不保证生效（引入后可能启动就报错）。
              而 PageHelper 本质是一个 MyBatis 插件（Interceptor），跟 Spring Boot 版本无关，
              手动注册成 Bean 就行 —— 见 config/MyBatisConfig。

            版本对应关系（查过 pom 确认的）：
              pagehelper 6.1.1 声明依赖 mybatis 3.5.19 —— 与本项目 mybatis 版本一致。
              它唯一的 compile 传递依赖是 jsqlparser（本地已有），
              mybatis / guava / 各种连接池 都是 optional，不会额外拉包。
        -->
        <dependency>
            <groupId>com.github.pagehelper</groupId>
            <artifactId>pagehelper</artifactId>
            <version>6.1.1</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!--
            AOP —— Spring Boot 4 里这个 starter 改名了！

            老名字（网上教程、Boot 3 及以前）：spring-boot-starter-aop
            新名字（Boot 4 起）：            spring-boot-starter-aspectj
            写老名字会直接报 "Could not find artifact"。

            它只拉三个东西（查过 4.1.0 的 pom 确认）：
              spring-boot-starter  4.1.0
              spring-aop           7.0.8
              aspectjweaver        1.9.25.1   ← 真正干活的字节码织入工具

            注意：这个 starter 是「安全的」，不会像 spring-boot-starter-security 那样
                  一加就把所有接口锁起来 —— 它只是让 @Aspect 生效，什么都不做就没有任何影响。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aspectj</artifactId>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>org.projectlombok</groupId>
                                    <artifactId>lombok</artifactId>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </execution>
                    <execution>
                        <id>default-testCompile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                        <configuration>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>org.projectlombok</groupId>
                                    <artifactId>lombok</artifactId>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
